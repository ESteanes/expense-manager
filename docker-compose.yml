services:
  app:
    image: up-bank-go:latest
    container_name: up-bank-go
    ports:
      - "8080:8080"
    environment:
      UP_BANK_TOKEN: ${UP_BANK_TOKEN}

secrets:
  UP_BANK_TOKEN:
    environment: ${UP_BANK_TOKEN:?error}
